#include <bits/stdc++.h>
#include "print_array_recursive.cpp"

using namespace std;
typedef long long ll;

int main() {
	ll size = 5;
	ll array[] = {10, 20, 30, 40, 50};

	print_array(array, size);
	print_array(array, size);
	return 0;
}